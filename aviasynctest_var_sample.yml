---
# Cluster IP for Avi Controller
controller_cluster_ip: 172.16.20.61

# Controller Credentials
avi_credentials:
  controller: "{{ controller_cluster_ip }}" # This can be either cluster ip or the ip of the leader controller.
  username: "<user>"
  password: "<password>"
  api_version: "22.1.6"

# Controller Credentials
avi_credentials:
  controller: "{{ controller_cluster_ip }}" # This can be either cluster ip or the ip of the leader controller.
  username: "<user>"
  password: "<password>"
  api_version: "30.2.1"

# Enable/Disable extra informative messages
debug: true
