---
# Cluster IP/hostname for Avi Controller
controller_cluster_ip: 172.16.20.22

# Controller Credentials
avi_credentials:
  controller: "{{ controller_cluster_ip }}" # This can be either cluster ip or the ip of the leader controller.
  username: "admin"
  password: "<password>"
  api_version: "30.2.1"


# Enable extra output
debug: true